<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="onboarding_sms_template" model="send_sms">
            <field name="name">Onboarding_SMS</field>
            <field name="gateway_id" model="gateway_setup" search="[('name','=','Bulksms_Gateway')]"/>
            <field name="model_id" ref="kw_onboarding.model_kwonboard_enrollment"/>
            <field name="sms_to">${object.mobile}</field>
            <field name="sms_html">Hello ${object.name}, your reference number is ${object.reference_no}.Please keep it secrete.</field>
       </record>
    </data>
</odoo>