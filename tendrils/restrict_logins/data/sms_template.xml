<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="restrict_login_otp_sms_template" model="send_sms">
            <field name="name">Login_OTP_SMS</field>
            <field name="gateway_id" model="gateway_setup" search="[('name','=','Bulksms_Gateway')]"/>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="sms_to">${object.partner_id.mobile}</field>
            <field name="sms_html">Hello ${object.partner_id.name}, your OTP is ${object.verification_code}.</field>
        </record>
    </data>
</odoo>