<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!--START : res.company, Add the customized field to existing view -->
        <record model="ir.ui.view" id="kw_res_company_form_view">
            <field name="name">res.company.view.form.inherit</field>
            <field name="model">res.company</field>
            <field name="inherit_id" ref="base.view_company_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='name']" position="after">
                    <field name="company_code" attrs="{'required': True}" placeholder="Company Code" />
                </xpath>
            </field>
        </record>
    </data>
</odoo>    