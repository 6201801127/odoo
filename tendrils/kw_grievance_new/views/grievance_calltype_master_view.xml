<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- <record id="view_grievance_calltype_master_search" model="ir.ui.view">
        <field name="name">grievance.calltype.master.search</field>
        <field name="model">grievance.calltype.master</field>
        <field name="arch" type="xml">
            <search string="grievance Call Type Search">
                <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
                <separator/>
                <field name="call_name" filter_domain="[('name', 'ilike', self)]"/>
            </search>
        </field>
    </record> -->

    <!-- <record id="view_grievance_category_form" model="ir.ui.view">
        <field name="name">view.grievance_category.form</field>
        <field name="model">grievance.calltype.master</field>
        <field name="arch" type="xml">
            <form string="Call Type">
                <header>
                </header>
                <sheet>
                    <group name="main">
                        <field name="call_name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_grievance_calltype_master_tree" model="ir.ui.view">
        <field name="name">view.grievance.calltype.master.tree</field>
        <field name="model">grievance.calltype.master</field>
        <field name="arch" type="xml">
            <tree string="Call Type">
                <field name="call_name"/>
            </tree>
        </field>
    </record> -->

</odoo>
