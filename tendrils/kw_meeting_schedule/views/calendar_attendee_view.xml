<?xml version="1.0"?>
<odoo>
  <data>
   
    
    <!-- <record model="ir.ui.view" id="kw_meeting_schedule_attendance_view_tree">
      <field name="name">meeting_schedule calendar.attendee list </field>
      <field name="model">kw_meeting_attendee</field>
      <field name="arch" type="xml">
        <tree create="true" editable="top" >
          <field name="partner_id" readonly="1" invisible="1" />
          <field name="employee_id" string="Name" />
          <field name="email" string="E-mail" />
          <field name="state"  />
          <field name="attendance_status" string="Attended Meeting" />
         
        </tree>
      </field>
    </record> -->


<!--     <record id="action_window_meeting_schedule_attendace" model="ir.actions.act_window">
        <field name="name">Meeting Attendance</field>
        <field name="res_model">kw_meeting_attendee</field>
        <field name="view_type">form</field>
        <field name="target">new</field>
        <field name="domain">
          [
            ("event_id", "=",  active_id),
          ]
        </field>
        <field name="context">
          {
            
            'default_event_id': active_id,
          }
          </field>
    </record> -->

    </data>
</odoo>
