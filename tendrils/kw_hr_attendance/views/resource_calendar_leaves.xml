<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        

        <record id="resource_calendar_leave_optional_holiday_tree" model="ir.ui.view">
            <field name="name">resource.calendar.leaves.tree.optional holiday</field>
            <field name="model">resource.calendar.leaves</field>
            <field name="inherit_id" ref="resource.resource_calendar_leave_tree"/>
            <field name="arch" type="xml">
                <field name="date_to" position="after">
                    <!-- <field name="optional_holiday" string="Optional Leave" /> -->
                    <field name="start_date" string="Date" />
                </field>
            </field>
        </record>

   
    </data>
</odoo>
