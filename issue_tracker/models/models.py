# -*- coding: utf-8 -*-

from odoo import models, fields, api


class issue_tracker(models.Model):
    _name = 'issue_tracker.issue_tracker'
    _description = 'issue_tracker.issue_tracker'

    # name = fields.Char()
    # value = fields.Integer()
    # value2 = fields.Float(compute="_value_pc", store=True)
    # description = fields.Text()


