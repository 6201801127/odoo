<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="project_view_project_form_inherit" model="ir.ui.view">
        <field name="name">project.project.form.views.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet//div[@name='button_box']//button[2]" position="after">
                <button class="oe_stat_button" name="open_project_issues" type="object" icon="fa-bug">
                    <field string="Issues" name="issue_count" widget="statinfo"/>
                </button>
            </xpath>
        </field>
    </record>

    <record id="project.action_view_all_task" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">project.task</field>
        <field name="view_mode">tree,kanban,form,calendar,pivot,graph,activity</field>
        <field name="context">{'search_default_my_tasks': 1, 'all_task': 0}</field>
        <field name="domain">[('is_issue', '=', False), ('risk_incident', '=', False)]</field>
        <field name="search_view_id" ref="project.view_task_search_form"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No tasks found. Let's create one!
            </p>
            <p>
                To get things done, use activities and status on tasks.
                <br/>
                Chat in real time or by email to collaborate efficiently.
            </p>
        </field>
    </record>

</odoo>
