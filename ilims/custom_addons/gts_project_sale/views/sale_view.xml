<odoo>

    <record id="view_sale_order_inherit_form_views" model="ir.ui.view">
        <field name="name">sale.order.form.view.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="is_from_project" invisible="1"/>
                <field name="analytic_account_id" options="{'no_create': True, 'no_edit': True, 'no_open': True}"
                       string="Project Name"
                       attrs="{'readonly': ['|', ('state','=','sale'), ('is_from_project', '=', True)]}"/>
            </xpath>
            <xpath expr="//notebook//page[@name='other_information']//group[1]//group[2]//field[@name='analytic_account_id']"
                   position="attributes">
                <attribute name="attrs">{'readonly': ['|', ('state','=','sale'), ('is_from_project', '=', True)]}
                </attribute>
                <attribute name="options">{'no_create': True, 'no_edit': True, 'no_open': True}</attribute>
            </xpath>
        </field>
    </record>

</odoo>
