<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
          <record id="account_account_move_form_inherited" model="ir.ui.view">
            <field name="name">Account Move</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- wagisha -->
                <xpath expr="//sheet/group/group[2]/div[@name='journal_div']" position="after">
                    <field name="credit_limit_amount" readonly="1"/>
                </xpath>
            </field>
        </record>

         <!-- .KHUSBOO add filter... -->
    <record model="ir.ui.view" id="new_view_account_move_filter">
        <field name="name">new.view.account.move.filter</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="arch" type="xml">            
        <xpath expr="//field[@name='name']" position="after">
            <filter string="Today" name="today" domain="[('date', '=', context_today().strftime('%Y-%m-%d'))]" />
            <!-- <filter string="Monthly" name="monthly" domain="[('date', '=', context_today().strftime('%Y-%m-%d'))]" /> -->
            <filter name="last_week" string="Last Week" domain="[('date','>', (context_today() - datetime.timedelta(weeks=1)).strftime('%%Y-%%m-%%d') )]"/>
        </xpath>
        </field>
    </record>
        <!--KHUSBOO.-->

    <!--.KHUSBOO add field..-->
        <record id="new_view_partner_form" model="ir.ui.view">
            <field name="name">view partner form</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='vat']" position="after">
                    <field name="pan_no"/>
                    <field name="tin_no" />
                    <field name="gst" />
                </xpath>                
            </field>
        </record>
    <!--.KHUSBOO....-->
    
    </data>
</odoo>
