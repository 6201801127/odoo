<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="bsscl_exit_ir_rules_id" model="ir.rule">
            <field name="name">BSSCL Exit rules</field>
            <field name="model_id" ref="model_exit_transfer_management"/>
            <field name="domain_force">['|','|',('employee_id.user_id', '=', user.id),('employee_id.parent_id.user_id', '=', user.id),('create_uid','=',user.id)]</field>
            
        </record>

</odoo>
